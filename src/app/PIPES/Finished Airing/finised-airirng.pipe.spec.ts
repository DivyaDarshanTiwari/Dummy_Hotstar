import { FinisedAirirngPipe } from './finised-airirng.pipe';

describe('FinisedAirirngPipe', () => {
  it('create an instance', () => {
    const pipe = new FinisedAirirngPipe();
    expect(pipe).toBeTruthy();
  });
});
